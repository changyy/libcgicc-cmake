libcgicc-cmake
==============

error message:

~~~
$ ./configure
$ make
mv html/index.html html/index.html.bak
~~~

fix:

$ sudo apt-get install doxygen

or skip the document generation:

$ make install

